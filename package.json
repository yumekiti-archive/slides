{
  "config": {
    "basePath": "slides/demo"
  },
  "scripts": {
    "start": "fusuma start",
    "start:prod": "fusuma start-prod",
    "build": "fusuma build -b $npm_package_config_basePath -o ../../dist",
    "deploy": "npm run build && fusuma deploy",
    "pdf": "npm run build && fusuma pdf"
  },
  "devDependencies": {
    "fusuma": "^2.8.4"
  }
}